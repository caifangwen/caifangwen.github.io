• 建议保留两个仓库、分别部署：caifangwen.github.io 负责根目录文章站，adelie-team 继续负责 /adelie-team/。 根目录文章站复
  用现有 Astro 的布局和样式。

  我看了代码，目前的问题是：

- Worker 把文章写进 caifangwen.github.io/blog/、notes/ 等目录，却通知 adelie-team 重新部署。

- adelie-team 把文章仓库拉到 src/content/docs，通过 docs/[...slug].astro 渲染。

- 加上目前的 base: '/adelie-team'，文章就会出现在 /adelie-team/docs/blog/文章名，而不是根目录分类下。
  
  推荐最终地址结构：
  
  www.261449.xyz/
  ├── blog/                       博客分类列表
  │   └── 2026-09-12-文章名/       Astro 渲染的文章
  ├── notes/                      笔记分类列表
  ├── projects/                   项目分类列表
  ├── docs/                       文档分类列表
  ├── inbox/                      收件分类列表
  └── adelie-team/                现有团队网站
  
  具体分四处处理：
1. 根目录仓库接入 Astro。
   在 caifangwen.github.io 中增加 Astro 构建，复用这里的文章布局、样式和组件。内容集合只读取 blog/notes/projects/docs/
   inbox 中的 Markdown；按目录生成分类列表和文章页。首页展示分类和最新文章。已有 Markdown 不必搬家。

2. Worker 改为触发文章站构建。
   保留当前写文件逻辑，把 SITE_REPO 从 adelie-team 改成 caifangwen.github.io，并在该仓库的部署 workflow 中监听 email-
   content-updated。同时补齐文章的 category 和 publishDate，旧文章可以从目录和文件名前缀推导。

3. 团队站保留现有部署位置。
   E:\adelie 的 base: '/adelie-team' 保留。取消它构建时拉取整个文章仓库的步骤，导航中的文章入口改为指向根目录分类。已
   有 /adelie-team/docs/... 链接若在使用，应补迁移跳转。

4. 自定义域名绑定根站。
   将 www.261449.xyz 配在 caifangwen.github.io 仓库的 Pages 设置中，www 的 DNS CNAME 指向
   caifangwen.github.io；团队仓库不单独绑定这个域名。GitHub 支持项目站继承根站域名，以 /adelie-team 路径访问。GitHub
   官方说明
   
   根站 Astro 使用 base: '/'，site 使用最终主域名；团队站继续使用 /adelie-team。绑定自定义域名后，github.io 通常会跳转到
   自定义域名，应选一个作为正式地址。Astro 部署说明
   
   关键是让根站仓库负责构建文章，而不是只把现有团队站的 base 改成 /。 目前我只检查了本地代码，尚未修改文件或线上配置。
